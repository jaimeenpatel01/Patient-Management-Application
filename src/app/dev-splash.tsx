import DevSplashScreen from '@/components/DevSplashScreen';

/**
 * Dev-only route: /dev-splash
 *
 * Navigate here to see the splash screen design with hot reload.
 * Once you finalize the design, export it as a static image
 * and remove this file.
 */
export default function DevSplashRoute() {
  return <DevSplashScreen />;
}
